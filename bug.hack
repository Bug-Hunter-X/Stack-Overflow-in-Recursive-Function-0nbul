function foo(x: int): int {
  if (x == 0) {
    return 1;
  } else {
    return x * foo(x - 1);
  }
}

function main(): void {
  echo foo(5);
}

This code has a potential stack overflow error if the input x is a large negative number because the recursive calls will never stop.